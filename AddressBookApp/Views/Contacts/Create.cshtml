@model AddressBookApp.ViewModels.ContactFormViewModel

@{
    ViewBag.Title = "Manage contact";
}

<div class="container">
    <div class="row">
        <div class="col-xs-12">
            @Html.ActionLink("Back to List", "Index")
        </div>
    </div>
    @{ Html.RenderPartial("Partials/Contacts/Panels/_ContactDetailsPanel", Model);}
    <div class="row">
        @{ Html.RenderPartial("Partials/Contacts/Panels/_ContactEmailsPanel", Model);}
        @{ Html.RenderPartial("Partials/Contacts/Panels/_ContactAddressesPanel", Model);}        
    </div>
</div>

